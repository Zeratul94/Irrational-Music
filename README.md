# Irrational-Music

Download my sketchy .exe file and try out the generation! It knows the irrationals pi, e, phi (golden ratio), and natural logs, and it can interpret regular decimal numbers, too!

You'll need a MIDI environment set up to receive messages and play sound, since this program only handles the MIDI end.